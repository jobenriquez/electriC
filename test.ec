Main
{ 
  int f, x = (d + x)+s*3;
  string z = CMTH(3);
  int y = Scan();
  string y = Scan();
  
  float ans;
  ans = CW2(5,y);

  PrintLine(ans);
 
  for(int i=2; i<=100; i--)
  {
    Print("Hello World");
  } 

  x = Scan();

  sx = 2;
  boolean x = false;
  bool x = true;
  character t = 'x';

  float d = 3.2;
  integer k=0;

  while(x!=3)
  {
    Print("Hi");
  }

  do 
  {
    PrintLine(x);
  }while(d < 100); 

  for(int i=2; i<=100; i--)
  {
    Print("Hello World");
  } 

  if(i>3)
  {
    Print(x);
  }
  elseif(d>x)
  {
    PrintLine(x);
  }
  else
  {
    PrintLine("Hello World");
  } 

  if(i>3)
  {
    return x;
    return CW2(x, 6);
    return "Hi";
    return 2;
    return true;
  }
  

  int x = 23;
  if (x > 10)
  {
    Print("x is larger than 10");
  }

  int count = 1;

  while (count <= 5)
  {
    Print("This is
    iteration #" + 
    count);

    count++;
  }

  int count = 1;
  
  do
  {
    Print("This is
    iteration #" +
    count);

    count++;
  } while (count <= 5);

  for (int i = 1; i <= 5; i++)
  {
    for (int j = 1; j <= i; j++)
    {
      Print("* ");
    }
    PrintLine(); 
  }

  int x = 5;

  for (int i = 0; i < x; i++)
  {
    Print("Hello world");
  }

  int x = 12;

  if (x < 20)
  {
    if (x > 10)
    {
      Print("x is less than 20 but greater than 10");
    }
    else
    {
      Print("x is less than 20 and is also less than 10");
    }
  }
  else
  {
    Print("x is greater than 20");
  }

  int x = 2;

  if (x > 10)
  {
    Print("x is larger than 10");
  }
  else
  {
    Print("x is smaller than 10");
  }

  int x = 23;

  if (x > 10)
  {
    Print("x is larger than 10");
  } 

  string name = Scan();

  Print("Hello, " + name);

 
 Print(4 + 4 * 5);
}
